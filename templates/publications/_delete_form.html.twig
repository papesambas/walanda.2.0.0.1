<form method="post" action="{{ path('publications_app_delete', {'slug': publication.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ publication.slug) }}">
	<button class="btn">Delete</button>
</form>
